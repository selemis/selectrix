package com.selectrix.plugin;

/**
 * Base class for plugin processing context.
 * Plugins can extend this class to create their own context objects
 * that carry state through the processing lifecycle.
 */
public abstract class ProcessingContext {
    // Base class for plugin-specific context objects
    // Can add common utilities here in the future if needed
}
